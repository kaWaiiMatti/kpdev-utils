# kpdev-ado-agent
Container image for self hosted Azure DevOps agent. The image will utilize federated identity to authenticate and relies on a pre-configured service principal.

Following applications are guaranteed to be there:
- az - Azure CLI

Other applications may be there as well